pipeline:
  test:
    image: ianmaguire/trigger-warning
    commands:
      - echo "Hello world!"

  publish:
    image: github.com/ianmaguire/triggered
    secrets: [im_token,api_key]
    files: version
    when:
      event: push
      branch: master
      success: true